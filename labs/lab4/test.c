#include <stdio.h>

int main() {

/*	int a, b, rem;

	printf("Enter two integers: ");
	scanf("%d %d", &a, &b);

	rem = a % b;

	printf("mod: %d\n", rem);
*/
/*	int n, sqr;
	printf("enter an odd number: ");
	scanf("%d", &sqr);

	int i = 0; 
	int j = sqr/2;
	int count = 1;
	for (n = 1; n <= (sqr*sqr); n++) {
		printf("%d: a[%d][%d]\n", n, i, j);
		if (count == sqr) {
			i++;
			count = 1;
		} else {
			i = ((--i % sqr) == -1) ? sqr - 1 : i;
			j = ((++j % sqr) == 0) ? 0 : j;
			count++;
		}
	}
			
*/
}
